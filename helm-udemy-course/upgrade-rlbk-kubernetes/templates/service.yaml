apiVersion: v1
kind: Service
metadata:
  name: {{ include "upgrade-rlbk-kubernetes.fullname" . }}
  labels:
    {{- include "upgrade-rlbk-kubernetes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upgrade-rlbk-kubernetes.selectorLabels" . | nindent 4 }}
