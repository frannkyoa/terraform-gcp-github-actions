apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "upgrade-rlbk-kubernetes.fullname" . }}-test-connection"
  labels:
    {{- include "upgrade-rlbk-kubernetes.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "upgrade-rlbk-kubernetes.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
